# Frontend Environment Variables
# Copy this file to .env.local for local development or .env.production for production

# ============================================================================
# API Configuration
# ============================================================================
# API URL for backend server
# IMPORTANT: Do NOT include trailing slash - it will be automatically normalized
# 
# Local Development:
VITE_API_URL=http://localhost:3001

# Production (uncomment and set for production builds):
# VITE_API_URL=https://pa.duhanashrah.ai

# ============================================================================
# Google OAuth Configuration
# ============================================================================
# Google OAuth Client ID for Gmail Integration
# This should match the GOOGLE_CLIENT_ID in backend/.env
# Get this from: Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
VITE_GOOGLE_CLIENT_ID=699573195201-n5sfj8j4t4pvnir82ehn5q24u6n8nuu6.apps.googleusercontent.com

# ============================================================================
# Supabase Configuration (if using Supabase)
# ============================================================================
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# ============================================================================
# Notes
# ============================================================================
# - Vite requires the VITE_ prefix for environment variables to be exposed to the frontend
# - Never expose secrets in frontend environment variables - only public values like Client IDs
# - For production, set VITE_API_URL to your production backend URL (without trailing slash)
# - If VITE_API_URL is not set in production, the app will use relative URLs (same domain)

